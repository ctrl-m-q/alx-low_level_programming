#include <stdio.h>
/**
 * main - C program that prints exactly a line followed by a line(use printf)
 * Return:Always returns (0)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
