#include <stdio.h>

/**
 * main - C program that prints line folowed by a new line
 * Return: Always return 0 (Success)
 */

int main(void)
{
	puts ("\" Programming is like building a multilingual puzzle");
	return (0);
}
